<div class="container">
	<div class="bg">
		<div class="row">
			<div class="col-sm-10">
				<div class="contact-form">
					<% if(flash && flash.err) { %>
					<ul class="alert alert-success">
						<% Object.keys(flash.err).forEach(function(error){ %>
						<li> <%- JSON.stringify(flash.err[error]) %></li>
						<% }) %>
					</ul>
					<% } %>
					<h2 class="title text-center">Imagen Producto</h2>
					<div class="status alert alert-success" style="display: none"></div>
					<form  enctype="multipart/form-data" id="main-contact-form" class="contact-form row" method="post" action="/imagenproducto/upload/<%=producto.id%>">
						<div class="form-group col-md-10">
							<label>Agregar imagen</label>
							<input type="file" name="imagenProducto" />
						</div>
						<div class="form-group col-md-3">
							<div class="form-group col-md-10">
								<input type="submit" name="submit" class="btn btn-primary pull-right" value="Guardar">
							</div>
						</form>
						<a href="/producto/show/<%=producto.id%>" class="btn btn-primary pull-right">Volver</a>
					</div>
				</div>
			</div>
		</div>
	</div>